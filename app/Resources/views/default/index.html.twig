{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
                <h2>Tasks</h2>
                <div class="well">
                    {{ include('tasks/form.html.twig', {'form': form}) }}
                </div>
                <hr>

                {{ include('tasks/list.html.twig', {'tasks': tasks}) }}

            </div>
            <div class="col-md-3"></div>
        </div>
    </div>
{% endblock %}
